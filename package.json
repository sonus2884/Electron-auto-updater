{
  "name": "electron-updater-example",
  "version": "1.0.2",
  "main": "main.js",
  "description": "electron-updater example project",
  "author": "",
  "scripts": {
    "publish": "electron-builder -p always"
  },
  "devDependencies": {
    "electron": "5.0.7",
    "electron-builder": "21.2.0"
  },
  "dependencies": {
    "electron-updater": "^4.0.6"
  },
  "build": {
    "publish": {
      "provider": "s3",
      "bucket": "usthaan-test-bucket"
    },
    "snap": {
      "publish": {
        "provider": "generic",
        "url": "https://usthan.com"
      }
    }
  }
}
